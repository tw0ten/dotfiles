#!/bin/bash
[[ -f ~/.bashrc ]] && . ~/.bashrc

neofetch
pacman -V
lsblk
ls -la /

while true; do
	echo -e "\n${cyan}start[x]\n${yellow}[u]pdate\n${grey}[c]ancel\n$reset"
	read -s -n1 opt
	case $opt in
		"u") paru || sudo pacman -Syu ;;
		"x") startx; break ;;
		"c") break ;;
		*) ;;
	esac
done
