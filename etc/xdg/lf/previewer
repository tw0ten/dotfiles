#!/bin/sh
set +e
mime="$(xdg-mime query filetype "$1" || echo '*/*')"

tput setaf 2
echo "$mime"
tput setaf 15
echo

case "$mime" in
'image'/*) jp2a --colors "$1" ;;
*) false ;;
esac && exit

grep -Iq . "$1" && exec cat "$1"

tput rev
echo 'binary'
