#!/bin/sh
poll() {
	wpctl get-volume @DEFAULT_AUDIO_SINK@ |
		sed -e 's/.*: /"volume":/' -e 's/ .*/,"muted":true/' \
			-e 's/^/{/' -e 's/$/}/'
}

poll
pactl subscribe |
	while read -r i; do
		case "$i" in
		"Event 'change' on sink"*) poll ;;
		esac
	done
