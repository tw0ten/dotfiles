#!/bin/sh
set -eu
cd "$DOTFILES/bin/s"
mkdir -p "$HOME/.cache/s"
[ "$*" = '' ] && set -- "$(find . -type f | sed 's/..//' | sort | dmenu -p '~ ' -auto-select)"
cd "$OLDPWD"
i="$1"
shift
set +eu
. "$OLDPWD/$i"
