#!/bin/sh
i(){ brightnessctl get; }
max=$(brightnessctl max)

i=$(echo -e "5%-\n$(i)\n+5%\n0\n25%\n50%\n75%\n$max" | dmenu -p "brightness " --select-index 1 --lines 8)

brightnessctl set "$i"

notify-send -t 1500 'brightness' "set to $(i)"
systat-kill 'brightness'
