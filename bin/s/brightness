#!/bin/sh
set -e
cd "$DOTFILES/timer/brightness"
base=$(cat i/base)
max=$(brightnessctl max)

i=$(echo -e "$(brightnessctl get)\n0\n$base\n$max" | dmenu -p "brightness " --select-index 2 --lines 4)

echo $i > i/base

notify-send -t 1500 'brightness' "base = $i\ndelta = $(cat i/delta)"
