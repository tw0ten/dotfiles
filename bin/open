#!/bin/sh
[ -z "$*" ] && exec $TERMINAL
[ "$TERM" = 'linux' ] || unset TERMINAL
for i in "$@"; do (
	[ -d "$i" ] && exec $TERMINAL $FILES "$i"
	case "$i" in
		*://*) exec $BROWSER "$i" ;;
	esac
	[ -f "$i" ] || exec $TERMINAL $VISUAL "$i"
	exec xdg-open "$i"
) done
